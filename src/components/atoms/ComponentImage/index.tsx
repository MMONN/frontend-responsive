import { clsx } from "clsx";

export type ComponentImagePropsType = {
  sourceImage?: {
    desktopSiz?: string
    tableSize?: string
    mobileSize?: string
  };
};

const ComponentImage = ({
  sourceImage,
}: ComponentImagePropsType): JSX.Element => {
    // console.log('sourceImage',sourceImage?.desktopSiz)
  return (
    <>
     
     
    </>
  );
};

export default ComponentImage;
